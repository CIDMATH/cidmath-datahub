"""Reusable transformation utilities.

Subject-specific transforms can live here when they're general enough to be
shared, or in `bundles/<subject>/src/` when they're specific to one subject.
The line: if more than one subject would use it, it goes here.
"""
